// src/services/reportes.service.js

const { prisma } = require('../config/database');

/**
 * Servicios para generar reportes en PDF / Excel.
 */
const generarReporteHHEE = async (mes, anio) => {
    // TODO: implement logic
    return [];
};

module.exports = {
    generarReporteHHEE
};